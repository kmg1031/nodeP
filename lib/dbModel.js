const conn = require(`${__dirname}/connect`);

module.exports = {
    insert : (req,res) => {
        var query = ``;

        conn.query(query);
    },
    update : (req,res) =>{
        var query = ``;

        conn.query(query);
    },
    select : (req,res) => {
        var query = ``;

        conn.query(query);
    },
    delete : (req,res) => {
        var query = ``;

        conn.query(query);
    },
}
